import 'package:flutter/material.dart';

Container emailCouldNotBeVerified(BuildContext context) => Container(
        child: Column(
      children: [
        Text('EMAIL CANT BE VERIFIED!'),
      ],
    ));
